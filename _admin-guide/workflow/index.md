---
layout: default
title: Workflow
nav_order: 7
has_children: true
---

# Workflow

This section describes the related administration steps for Workflow in MCdesk.

## Overview

This section describes the following areas:

- Admin needs to setup a **Baseline Workflow** for Service Request

Workflows define how a request or ticket moves through states (e.g. approval, rejection) and who performs each step.

## Configure / Pre-requisite

- [Create a Baseline Service Request Workflow]({{site.url}}{{site.baseurl}}/admin-guide/workflow/create-baseline-workflow/) – Create a workflow that runs when a service request is submitted
